<center>	<h1>Listing developments</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr></center>

<% @developments.each do |development| %>
  <tr>
    <td><%= development.name %></td>
    <td><%= link_to 'Show', development %></td>
    <td><%= link_to 'Edit', edit_development_path(development) %></td>
	
    <td><%= link_to 'Destroy', development, :confirm => 'Are you sure?', :method => :delete %></td>
    <td><%= button_to 'Map', map_path %></td>
  </tr>
<% end %>
</table>

<br />
<%= link_to 'Add New Owner', new_owner_path%>
<%= link_to 'New Development', new_development_path %>
